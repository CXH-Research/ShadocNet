GPU: [0, 1, 2, 3]

VERBOSE: True

#MODEL:
#  MODE: 'Deraining'
#  SESSION: 'MPRNet'

# Optimization arguments.
OPTIM:
  TRAIN_BATCH_SIZE: 8
  TEST_BATCH_SIZE: 1
  NUM_EPOCHS: 5000
  # NEPOCH_DECAY: [10]
  LR_INITIAL: 2e-4
  LR_MIN: 1e-6
  # BETA1: 0.9

TRAINING:
  VAL_AFTER_EVERY: 1
  RESUME: False
  TRAIN_PS: 512
  VAL_PS: 512
  TRAIN_DIR: '../Jung/train'       # path to training data
  VAL_DIR: '../Jung/test' # path to validation data
#  SAVE_DIR: './checkpoints'           # path to save models and images
